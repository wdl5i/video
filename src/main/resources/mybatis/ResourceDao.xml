<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.monitor.video.dao.ResourceDao">


    <select id="list" resultType="com.monitor.video.vo.Resource">
        SELECT r.id, r.name FROM resource r LEFT JOIN user_resource ur where r.id = ur.resource_id and ur.user_id = #{userId} and r.status = 1
    </select>

    <select id="findIdByUrl" resultType="java.lang.Integer">
        SELECT id FROM resource WHERE status = 1 AND url like CONCAT('',#{url},'%')  AND method = #{method}
    </select>



    <!--<select id="findById" resultType="com.monitor.video.vo.User">-->
        <!--SELECT * FROM user where id = #{id}-->
    <!--</select>-->
</mapper>