<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.monitor.video.dao.UserDao">


    <select id="login" resultType="com.monitor.video.vo.User">
        SELECT * FROM user where name = #{name} and password = #{pwd}
    </select>

    <!--<select id="findById" resultType="com.monitor.video.vo.User">-->
        <!--SELECT * FROM user where id = #{id}-->
    <!--</select>-->
</mapper>